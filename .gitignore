# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

CLAUDE.md
CLAUDE.MD
AGENTS.md
AGENTS.MD
claude/*
.claude/
.agents/
.codex/

# Jekyll 빌드 산출물. 예전에 이름이 깨진 `_site\r` 디렉토리가 커밋된 적이 있어
# 접두사로 막는다.
_site*

# VSCode 워크스페이스 (로컬 설정)
*.code-workspace

POST-RULES.md
